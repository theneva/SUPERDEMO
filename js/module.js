// custom stuff uses 'angular', so must be defined after angular.js

// declare the module before registering stuff to it
angular.module('whatItSaysInNgApp', [
	// any dependencies like ngRoute go in this array
	'ngRoute'
]);
